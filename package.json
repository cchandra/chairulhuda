{
  "name": "chairul-huda-pusat-ilmu-pidana",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "description": "Pustaka hukum pidana, membership, dan pengelolaan publikasi Prof. Chairul Huda",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.mjs",
    "dev": "node --watch server.mjs",
    "build": "node --check server.mjs && node --check lib/db.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "ejs": "^3.1.10",
    "express": "^5.2.1",
    "multer": "^2.0.2"
  }
}
